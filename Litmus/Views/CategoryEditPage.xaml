<Page x:Class="Litmus.Views.CategoryEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Edit Category"
      Background="{StaticResource BackgroundDarkBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock x:Name="PageTitle" Text="New Category" Style="{StaticResource Header}"/>
        </StackPanel>

        <!-- Form -->
        <Border Grid.Row="1" Style="{StaticResource Card}">
            <StackPanel MaxWidth="400" HorizontalAlignment="Left">
                <TextBlock Text="Category Name" FontWeight="SemiBold" Margin="0,0,0,8"/>
                <TextBox x:Name="NameTextBox" MaxLength="200"/>
            </StackPanel>
        </Border>

        <!-- Actions -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,24,0,0">
            <Button Content="Cancel" Padding="16,8" Margin="0,0,12,0"
                    Click="Cancel_Click"/>
            <Button Content="Save Category" Style="{StaticResource PrimaryButton}" Padding="16,8"
                    Click="Save_Click"/>
        </StackPanel>
    </Grid>
</Page>
