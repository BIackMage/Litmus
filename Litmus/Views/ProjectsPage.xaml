<Page x:Class="Litmus.Views.ProjectsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Projects"
      Background="{StaticResource BackgroundDarkBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Projects" Style="{StaticResource Header}"/>
                <TextBlock Text="Manage your testing projects" Style="{StaticResource Muted}" Margin="0,4,0,0"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <CheckBox x:Name="ShowArchivedCheckbox" Content="Show Archived"
                          Foreground="{StaticResource ForegroundBrush}"
                          Margin="0,0,16,0"
                          Checked="ShowArchivedCheckbox_Changed"
                          Unchecked="ShowArchivedCheckbox_Changed"/>
                <Button Content="+ New Project" Style="{StaticResource PrimaryButton}"
                        Click="NewProject_Click"/>
            </StackPanel>
        </Grid>

        <!-- Projects List -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="ProjectsGrid" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column"
                      SelectionMode="Single"
                      MouseDoubleClick="ProjectsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name, Mode=OneWay}" Width="200"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description, Mode=OneWay}" Width="*"/>
                    <DataGridTextColumn Header="Categories" Binding="{Binding CategoryCount, Mode=OneWay}" Width="100"/>
                    <DataGridTextColumn Header="Tests" Binding="{Binding TestCount, Mode=OneWay}" Width="100"/>
                    <DataGridTextColumn Header="Created" Binding="{Binding CreatedDate, Mode=OneWay, StringFormat='{}{0:MMM dd, yyyy}'}" Width="120"/>
                    <DataGridTemplateColumn Header="Actions" Width="150">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Edit" Padding="8,2" Margin="0,0,4,0"
                                            Click="EditProject_Click"/>
                                    <Button Content="{Binding ArchiveButtonText, Mode=OneWay}" Padding="8,2"
                                            Click="ArchiveProject_Click"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock x:Name="NoProjectsText" Grid.Row="2"
                   Text="No projects found. Create your first project!"
                   Style="{StaticResource Muted}"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
