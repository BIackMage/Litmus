<Page x:Class="Litmus.Views.SearchResultsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Search Results"
      Background="{StaticResource BackgroundDarkBrush}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Search Results for: " Style="{StaticResource Header}"/>
                <TextBlock x:Name="SearchQueryText" Style="{StaticResource Header}"
                           Foreground="{StaticResource AccentBrush}"/>
            </StackPanel>
            <TextBlock x:Name="ResultCountText" Style="{StaticResource Muted}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Results -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="ResultsGrid" AutoGenerateColumns="False"
                      IsReadOnly="True" HeadersVisibility="Column"
                      SelectionMode="Single"
                      MouseDoubleClick="ResultsGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Test Name" Binding="{Binding Name, Mode=OneWay}" Width="250"/>
                    <DataGridTextColumn Header="Category" Binding="{Binding CategoryName, Mode=OneWay}" Width="150"/>
                    <DataGridTextColumn Header="Project" Binding="{Binding ProjectName, Mode=OneWay}" Width="150"/>
                    <DataGridTextColumn Header="Description" Binding="{Binding Description, Mode=OneWay}" Width="*"/>
                    <DataGridTextColumn Header="Priority" Binding="{Binding Priority, Mode=OneWay}" Width="80"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <TextBlock x:Name="NoResultsText" Grid.Row="1"
                   Text="No tests found matching your search."
                   Style="{StaticResource Muted}"
                   VerticalAlignment="Center" HorizontalAlignment="Center"
                   Visibility="Collapsed"/>
    </Grid>
</Page>
